# 🌐 Public Builder.io API Key (safe to expose)
VITE_PUBLIC_BUILDER_KEY=your-builder-public-key-here

---

## 🔐 Environment Setup

To run this project locally, create a `.env` file based on `.env.example`:

```bash
cp .env.example .env
```

Inside your `.env` file, paste your **Builder.io public API key**:

```env
VITE_PUBLIC_BUILDER_KEY=your-real-key-here
```

You can find it in your Builder.io space under:
[https://www.builder.io/account/settings](https://www.builder.io/account/settings)

This key is **safe to expose** in frontend environments, but should not be used for write access.
